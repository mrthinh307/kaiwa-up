import { ProtectedRouteContentSkeleton } from "@/components/common/protected-route/protected-route-content-skeleton";

export default function ProtectedLoading() {
  return <ProtectedRouteContentSkeleton />;
}
