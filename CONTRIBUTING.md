# Contributing

- Create focused branches and pull requests.
- Keep FastAPI as the source of truth for the API contract.
- Regenerate `packages/api-client` whenever the OpenAPI schema changes.
- Do not commit secrets or local environment files.
